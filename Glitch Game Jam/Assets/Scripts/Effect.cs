using UnityEngine;

public class Effect : MonoBehaviour
{
    public void ApplyCardEffects(CardData cardData, GameObject target)
    {
        /*foreach (var consequence in card.consequences)
        {
            consequence.ApplyConsequence(target);
        }*/
    }
}
